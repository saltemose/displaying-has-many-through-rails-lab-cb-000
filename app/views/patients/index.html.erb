<h1>Patient Directory</h1>
<ul>
  <% @patients.each do |patient| %>
  <li> Name: <%= link_to patient.name, patient_path(patient) %> (<%= pluralize(patient.appointment_count, 'appointment')%>

  </li>
    <% end %>
</ul>
